name: Build & Check

on:
  pull_request:
    branches: [main]

jobs:
  build-check:
    name: Build, Type-check & Link-check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Astro build
        run: npm run build

      - name: Astro type-check
        run: npm run check

      - name: Link check (built output)
        run: npx linkinator ./dist --recurse --skip "^(?!http://localhost)"
        continue-on-error: true

  unit-tests:
    name: Unit Tests (Vitest)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm run test:unit

      - name: Check visual-baseline workflow contracts
        run: node --test tests/workflows/update-visual-baselines.test.mjs

  e2e-tests:
    name: E2E Tests (Playwright)
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.61.1-noble
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build for preview
        run: npm run build

      - name: Run E2E tests (smoke + terminal; excludes visual)
        run: npm run test:e2e

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

  visual-regression:
    # BLOCKING gate — compares against committed Linux baselines.
    # If baselines are stale (intentional UI change), run:
    #   Actions → Update Visual Baselines → branch: <your-pr-branch>
    # Prefer commit_baselines=false to review/download artifacts before committing.
    # Verify checks on the latest PR commit; bot events may need maintainer action.
    name: Visual Regression (Linux baselines)
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.61.1-noble
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build for preview
        run: npm run build

      - name: Run visual regression (compare mode)
        run: npm run test:visual

      - name: Upload visual diff artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: visual-snapshots
          path: tests/e2e/__snapshots__/
          retention-days: 14

